# AI integration layer.
# Phase 1: returns mock roadmap data (hardcoded).
# Phase 4: replaced with real OpenAI / Gemini API call.
# Keeping this in its own file means the rest of the code never changes
# when we swap mock for real AI.
