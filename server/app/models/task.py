# SQLAlchemy model for the tasks table.
# Defines columns: id, roadmap_id, user_id, title, description,
# week_number, day_number, scheduled_date, estimated_duration,
# is_completed, completed_at, created_at
